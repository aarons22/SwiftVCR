os: osx 
language: swift
osx_image: xcode10.1
xcode_workspace: VCR.xcworkspace
xcode_scheme: VCR-Example
xcode_destination: platform=iOS Simulator,OS=12.1,name=iPhone X
cache: cocoapods

before_install:
  - cd Example
  - pod repo update
